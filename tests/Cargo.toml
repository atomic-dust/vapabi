[package]
name = "vapabi-tests"
version = "0.1.1"
authors = ["debris <marek.kotewicz@gmail.com>"]
edition = "2018"

[dependencies]
vapabi = { path = "../vapabi" }
vapabi-derive = { path = "../derive" }
vapabi-contract = { path = "../contract" }
rustc-hex = "2.0"
hex-literal = "0.2.0"
